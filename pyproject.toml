[tool.uv.workspace]
members = [
    "libs/database",
    "libs/core",
    "tests",
    "libs/services",
]

[dependency-groups]
dev = [
    "ruff>=0.16.0",
]

[tool.ruff.lint]
ignore = [
    "BLE001",
]

[tool.behave]
paths = [
    "tests/features",
]
